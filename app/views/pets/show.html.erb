<div class="pet">
  <h2><%= @pet.name %></h2>
  <p>Breed: <%= @pet.breed %></p>
  <p>Age: <%= @pet.age %></p>
  <p>Shelter: <%= @pet.shelter_name %></p>
  <p class="links">
    <%= link_to "Edit", edit_pet_path(@pet) %>
    <%= link_to "Delete", pet_path(@pet), method: :delete %>
  </p>
</div>
